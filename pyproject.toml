[project]
name = "projet-spark"
version = "0.1.0"
description = "Stream infinis pour un site type le bon coin"
readme = "README.md"
requires-python = ">=3.11.3"
dependencies = [
    "fastapi>=0.136.3",
    "graphframes>=0.6",
    "pyspark==3.5.1",
]
